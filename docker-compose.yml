services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TEST_DB_PASSWORD=${TEST_DB_PASSWORD:-super_secret_123}
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
